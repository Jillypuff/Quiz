package Modules;

public enum ResponseType {
    CLIENT_CONNECTED,
    CLIENT_DISCONNECTED,

    QUEUE_JOINED,
    GAME_JOINED,
    GAME_OVER,

    CHOSE_CATEGORY,
    WAITING_FOR_CATEGORY_CHOICE,

    GAME_STARTED,
    SEND_ANSWER,

    SEND_SCORE,
    SEND_FINAL_RESULT,
}
