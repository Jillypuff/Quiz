package Modules;

public enum RequestType {
    CONNECT,
    DISCONNECT,
    START_GAME,
    EXIT_GAME,
    LEAVE_QUEUE,

    // CHANGE_USERNAME,
    // CHECK_HIGHSCORE,

    CATEGORY_CHOSEN,

    NEXT_ROUND,
    ROUND_RESULT,
    FINAL_RESULT,
    GIVE_UP,
}
